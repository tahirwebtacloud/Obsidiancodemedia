---
phase: 02-the-visual-overhaul
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend-next/app/identity/page.tsx
  - frontend-next/components/identity/IdentityBlocks.tsx
autonomous: true
requirements:
  - UI-05
must_haves:
  truths:
    - Identity tab centralizes Persona, Brand Knowledge, and Directives
    - Display uses structured/technical JSON-like visual blocks or strict cards
  artifacts:
    - frontend-next/app/identity/page.tsx
    - frontend-next/components/identity/IdentityBlocks.tsx
  key_links: []
---

<objective>
Build the Brand Identity Manager to act as the centralized hub for the user's playbook.

Purpose: To provide a structured, technical view of the AI constraints (Persona, Brand Knowledge, Output Directives) so the user explicitly sees the "code" guiding the generation.
Output: Identity page route and structured block components.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structured Identity Blocks</name>
  <files>frontend-next/components/identity/IdentityBlocks.tsx</files>
  <action>Create a specialized UI component (`IdentityBlocks`) that renders data in a structured / technical aesthetic (e.g., monospace font for labels, JSON-like bracket visuals, or strict rigid Shadcn cards). This should feel like a developer tool configuring the AI.</action>
  <verify>
    <automated>grep -q "IdentityBlocks" frontend-next/components/identity/IdentityBlocks.tsx || echo "Done"</automated>
  </verify>
  <done>Technical aesthetic blocks are ready for Identity data.</done>
</task>

<task type="auto">
  <name>Task 2: Identity Manager Page</name>
  <files>frontend-next/app/identity/page.tsx</files>
  <action>Create the `/identity` page route. Compose the page using `IdentityBlocks` to clearly separate three distinct sections: 'Persona', 'Brand Knowledge', and 'Output Directives'. Bind these views to read from the global `useAppStore` (or mock state initially) so changes are preserved.</action>
  <verify>
    <automated>grep -q "Persona" frontend-next/app/identity/page.tsx</automated>
  </verify>
  <done>Identity Manager is fully functional and centralized.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- The page renders the three required sections clearly
- The design feels structured and technical, matching the Obsidian Logic brand
</verification>

<success_criteria>
Brand Identity is completely centralized in one view without data loss.
</success_criteria>

<output>
After completion, create `.planning/phases/02-the-visual-overhaul/02-04-SUMMARY.md`
</output>