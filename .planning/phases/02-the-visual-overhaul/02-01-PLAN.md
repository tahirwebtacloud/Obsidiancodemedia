---
phase: 02-the-visual-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-next/app/layout.tsx
  - frontend-next/app/globals.css
  - frontend-next/components/Navigation.tsx
  - frontend-next/store/useAppStore.ts
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - User can navigate between Studio, Intelligence, Evolution, and Identity tabs
    - State is preserved globally using Zustand
    - Dynamic CSS theming is supported with Obsidian Logic default palette
  artifacts:
    - frontend-next/store/useAppStore.ts
    - frontend-next/components/Navigation.tsx
  key_links: []
---

<objective>
Build the core Application Shell, configure global state for progressive disclosure, and set up the dynamic theming system.

Purpose: To ensure seamless navigation between tabs without losing form input state (non-negotiable UX requirement) and to provide the Obsidian Logic aesthetic foundation with dynamic theme injection support.
Output: Zustand store, Navigation Shell component, configured global CSS.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global Theme System</name>
  <files>frontend-next/app/globals.css</files>
  <action>Update global CSS to include dynamic CSS variables for the Obsidian Logic palette. Set Obsidian Black (`#0E0E0E`) as the background and Signal Yellow (`#F9C74F`) as the primary accent. Ensure the architecture allows these variables to be overridden dynamically via inline styles on the body/root for backend injection. Strictly avoid "AI Slop" aesthetics.</action>
  <verify>
    <automated>grep -q "#0E0E0E" frontend-next/app/globals.css</automated>
  </verify>
  <done>CSS variables are established for the brand palette.</done>
</task>

<task type="auto">
  <name>Task 2: Global State Management Setup</name>
  <files>frontend-next/store/useAppStore.ts</files>
  <action>Create a Zustand global state store (install `zustand` if not present) to hold the state of Studio forms, Brand Identity configurations, and active navigation tab. This is required to preserve state when switching between tabs, preventing data loss.</action>
  <verify>
    <automated>npm list zustand --prefix frontend-next || npm install zustand --prefix frontend-next &amp;&amp; ls frontend-next/store/useAppStore.ts</automated>
  </verify>
  <done>Zustand store is created and exports `useAppStore` hook.</done>
</task>

<task type="auto">
  <name>Task 3: Application Shell &amp; Navigation</name>
  <files>frontend-next/app/layout.tsx, frontend-next/components/Navigation.tsx</files>
  <action>Build a progressive disclosure Navigation component (Sidebar or Tab bar based on desktop/mobile). Update `layout.tsx` to include the Navigation and wrap the main children. Ensure navigating between routes (`/studio`, `/intelligence`, `/evolution`, `/identity`) works cleanly within the Next.js App Router.</action>
  <verify>
    <automated>grep -q "Navigation" frontend-next/app/layout.tsx</automated>
  </verify>
  <done>The Next.js layout incorporates global navigation connecting to all Phase 2 views.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds in `frontend-next/`
- Navigation component is rendering in layout
- Zustand is correctly configured
</verification>

<success_criteria>
App Shell is ready for content pages with state persistence capabilities.
</success_criteria>

<output>
After completion, create `.planning/phases/02-the-visual-overhaul/02-01-SUMMARY.md`
</output>