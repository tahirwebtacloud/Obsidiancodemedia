---
phase: 02-the-visual-overhaul
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend-next/app/intelligence/page.tsx
  - frontend-next/app/evolution/page.tsx
  - frontend-next/components/evolution/MetricsChart.tsx
autonomous: true
requirements:
  - UI-03
  - UI-04
must_haves:
  truths:
    - Intelligence dashboard displays Viral and Competitor views
    - Evolution dashboard renders modern charts (Recharts/Tremor)
    - "Winning Hook" and "Winning CTA" callout boxes are visible
  artifacts:
    - frontend-next/app/intelligence/page.tsx
    - frontend-next/app/evolution/page.tsx
    - frontend-next/components/evolution/MetricsChart.tsx
  key_links: []
---

<objective>
Build the Intelligence and Evolution dashboards to visualize data and system progress.

Purpose: To provide clear, high-contrast insights into Viral/Competitor data and show the mathematical evolution of the user's content strategy via Impression growth charts and Winning Hook/CTA callouts.
Output: Two page routes and charting components.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Intelligence Dashboard</name>
  <files>frontend-next/app/intelligence/page.tsx</files>
  <action>Create the `/intelligence` page route. Build layout containers using Shadcn cards that divide the view into "Viral Posts" and "Competitor Analysis" sections. For now, render mock data or placeholder empty states that adhere to the bold, distinct Obsidian Logic visual design.</action>
  <verify>
    <automated>grep -q "Viral" frontend-next/app/intelligence/page.tsx</automated>
  </verify>
  <done>Intelligence tab structure is created.</done>
</task>

<task type="auto">
  <name>Task 2: Evolution Charts</name>
  <files>frontend-next/components/evolution/MetricsChart.tsx</files>
  <action>Install `recharts` (or `@tremor/react` if preferred and compatible). Create a LineChart component that visualizes "Impression Growth" over time, styled with the Signal Yellow accent line against the dark background. Feed it mock JSON data demonstrating an upward trend.</action>
  <verify>
    <automated>npm list recharts --prefix frontend-next || npm install recharts --prefix frontend-next &amp;&amp; ls frontend-next/components/evolution/MetricsChart.tsx</automated>
  </verify>
  <done>Chart component is ready for rendering metrics.</done>
</task>

<task type="auto">
  <name>Task 3: Evolution Dashboard &amp; Callouts</name>
  <files>frontend-next/app/evolution/page.tsx</files>
  <action>Create the `/evolution` page route. Import the `MetricsChart` at the top. Below the chart, implement explicit, highly-visible Shadcn callout cards/boxes for "Winning Hook" and "Winning CTA". Use aggressive, high-contrast styling to draw attention to these algorithmically determined winners.</action>
  <verify>
    <automated>grep -q "Winning Hook" frontend-next/app/evolution/page.tsx</automated>
  </verify>
  <done>Evolution page renders charts and explicit metric callouts.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Intelligence and Evolution pages are navigable
- Charts render without React hydration errors
</verification>

<success_criteria>
Intelligence and Evolution dashboards are visually complete and ready for data binding.
</success_criteria>

<output>
After completion, create `.planning/phases/02-the-visual-overhaul/02-03-SUMMARY.md`
</output>