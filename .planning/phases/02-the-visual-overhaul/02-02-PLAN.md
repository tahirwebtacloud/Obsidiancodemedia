---
phase: 02-the-visual-overhaul
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend-next/app/studio/page.tsx
  - frontend-next/components/studio/StudioForm.tsx
  - frontend-next/components/studio/Canvas.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - Desktop shows side-by-side view (Form left, Canvas right)
    - Mobile shows Form first, switches to Canvas on submission
    - Generation triggers immediate shimmering skeleton
    - Canvas streams SSE text in real-time
  artifacts:
    - frontend-next/app/studio/page.tsx
    - frontend-next/components/studio/Canvas.tsx
  key_links: []
---

<objective>
Build the Studio tab with a responsive split-screen layout, state-persisting forms, and a live SSE-streaming Canvas that masks latency with instant shimmering skeletons.

Purpose: To provide the core content generation interface, ensuring a high-quality visual experience that immediately responds to user input via skeleton loaders and real-time streaming text.
Output: Studio page route, Form component, Canvas component.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Studio Form Integration</name>
  <files>frontend-next/components/studio/StudioForm.tsx</files>
  <action>Build the generation form using standard Shadcn UI text inputs and dropdowns. Bind all inputs to the `useAppStore` global state so data is preserved if the user navigates away. On submit, trigger a state update that sets `isGenerating` to true and switches the mobile view to the Canvas.</action>
  <verify>
    <automated>grep -q "useAppStore" frontend-next/components/studio/StudioForm.tsx</automated>
  </verify>
  <done>Form captures user intent and stores it globally, ready to trigger generation.</done>
</task>

<task type="auto">
  <name>Task 2: Shimmering Canvas &amp; SSE Streaming</name>
  <files>frontend-next/components/studio/Canvas.tsx</files>
  <action>Build the Canvas component. When `isGenerating` is true, immediately display a shimmering skeleton (using Shadcn Skeleton component) to mask latency. Enhance the existing API fetch logic to consume Server-Sent Events (SSE) from the proxy endpoint (`/api/generate`), replacing the skeleton with real-time text streaming as chunks arrive.</action>
  <verify>
    <automated>grep -q "Skeleton" frontend-next/components/studio/Canvas.tsx</automated>
  </verify>
  <done>Canvas immediately reacts to generation and streams text directly from the API.</done>
</task>

<task type="auto">
  <name>Task 3: Responsive Studio Layout</name>
  <files>frontend-next/app/studio/page.tsx</files>
  <action>Compose the `StudioForm` and `Canvas` into the main `/studio` page route. Implement responsive split-screen: on Desktop (lg+), show side-by-side (Form left, Canvas right). On Mobile, show Form first, and upon generation submission, slide/switch the view entirely to the Canvas. Connect to the layout wrapper.</action>
  <verify>
    <automated>grep -q "StudioForm" frontend-next/app/studio/page.tsx</automated>
  </verify>
  <done>Studio layout behaves perfectly across device breakpoints.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- SSE logic is properly configured using the App Router proxy structure
- Skeletons are visually present
</verification>

<success_criteria>
Studio tab is fully functional with responsive layouts and streaming output.
</success_criteria>

<output>
After completion, create `.planning/phases/02-the-visual-overhaul/02-02-SUMMARY.md`
</output>