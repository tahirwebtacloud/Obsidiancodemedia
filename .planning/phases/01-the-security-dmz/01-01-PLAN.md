---
phase: 01-the-security-dmz
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-next/package.json
  - frontend-next/utils/supabase/server.ts
  - frontend-next/utils/supabase/client.ts
  - frontend-next/utils/supabase/middleware.ts
  - frontend-next/app/auth/callback/route.ts
  - frontend-next/app/login/page.tsx
  - frontend-next/app/page.tsx
  - frontend-next/.env.local
autonomous: false
requirements: [SEC-01, SEC-02]

must_haves:
  truths:
    - "Next.js App Router application runs successfully"
    - "User can authenticate via Supabase"
    - "Authentication state persists via HTTP-only cookies"
  artifacts:
    - path: "frontend-next/package.json"
      provides: "Next.js application"
    - path: "frontend-next/utils/supabase/server.ts"
      provides: "Supabase SSR client"
  key_links:
    - from: "Browser"
      to: "Next.js App Route"
      via: "HTTP-only auth cookie"
---

<objective>
Initialize Next.js App Router workspace and configure Supabase SSR Auth to use secure HTTP-only cookies.

Purpose: Establish the public-facing DMZ and secure authentication to replace the insecure Vanilla JS setup.
Output: A working Next.js App Router project with Supabase Auth integrated.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js Workspace</name>
  <files>frontend-next/package.json, frontend-next/tailwind.config.ts, frontend-next/app/globals.css</files>
  <action>
    Use `npx create-next-app@latest frontend-next --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --yes`.
    Once created, CD into `frontend-next` and install `@supabase/supabase-js` and `@supabase/ssr`.
    Install Shadcn UI dependencies (`npx shadcn@latest init -d`).
  </action>
  <verify>
    <automated>cd frontend-next && npm run build</automated>
  </verify>
  <done>Next.js builds successfully with Tailwind and Supabase packages installed.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Supabase SSR Utilities</name>
  <files>frontend-next/utils/supabase/server.ts, frontend-next/utils/supabase/client.ts, frontend-next/utils/supabase/middleware.ts</files>
  <action>
    Implement Supabase SSR clients according to official Supabase docs.
    - `client.ts`: `createBrowserClient`
    - `server.ts`: `createServerClient` with cookie management logic (get, set, remove).
    - `middleware.ts`: Function to refresh auth sessions on routes.
  </action>
  <verify>
    <automated>cd frontend-next && npx tsc --noEmit</automated>
  </verify>
  <done>Type-safe Supabase SSR utility files are created and compile without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Login Page & Auth Callback</name>
  <files>frontend-next/app/login/page.tsx, frontend-next/app/auth/callback/route.ts, frontend-next/app/page.tsx, frontend-next/.env.local</files>
  <action>
    Create a basic login page at `app/login/page.tsx` that supports Email/Password and OAuth (Google) via Supabase SSR.
    Create the PKCE callback route at `app/auth/callback/route.ts` that exchanges auth codes for sessions and sets HTTP-only cookies.
    Update `app/page.tsx` to read the server-side session and display "Logged in as [email]" or redirect to login.
    Create `.env.local` template with required Supabase variables.
  </action>
  <verify>
    <automated>cd frontend-next && npm run build</automated>
  </verify>
  <done>Authentication routes compile and correctly leverage the SSR utilities.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint - Human Verify</name>
  <action>Pause for human verification.</action>
  <files>N/A</files>
  <verify>Human visual check</verify>
  <done>Human approved</done>
  <what-built>Next.js foundation and Supabase SSR authentication.</what-built>
  <how-to-verify>
    1. Populate `frontend-next/.env.local` with Supabase URL and Anon Key.
    2. Start the app `cd frontend-next && npm run dev`.
    3. Visit `http://localhost:3000/login` and authenticate.
    4. Verify redirection to home page and verify `sb-...-auth-token` HTTP-only cookies are set in browser DevTools.
  </how-to-verify>
  <resume-signal>Type "approved" once you have successfully logged in and verified cookies.</resume-signal>
</task>

</tasks>

<verification>
Check that the Next.js app builds and the user session is correctly tied to an HTTP-only cookie.
</verification>

<success_criteria>
Next.js app is runnable, Supabase SSR correctly issues HTTP-only cookies, and user state is readable server-side.
</success_criteria>

<output>
After completion, create `.planning/phases/01-the-security-dmz/01-01-SUMMARY.md`
</output>
