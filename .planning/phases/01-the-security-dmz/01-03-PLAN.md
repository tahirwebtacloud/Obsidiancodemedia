---
phase: 01-the-security-dmz
plan: 3
type: execute
wave: 2
depends_on: [1, 2]
files_modified:
  - frontend-next/middleware.ts
  - frontend-next/lib/validations/api.ts
  - frontend-next/app/api/generate/route.ts
  - frontend-next/package.json
autonomous: false
requirements: [SEC-04, SEC-05]
user_setup:
  - service: "Upstash Redis"
    why: "Required for API rate limiting"
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Upstash Dashboard -> Redis Database -> REST API"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Upstash Dashboard -> Redis Database -> REST API"

must_haves:
  truths:
    - "Invalid API payloads are rejected by Zod before reaching Python"
    - "API requests are rate limited via Upstash Redis"
    - "Next.js proxies valid requests to the Python server with JWT attached"
  artifacts:
    - path: "frontend-next/lib/validations/api.ts"
      provides: "Zod schemas for generation inputs"
    - path: "frontend-next/middleware.ts"
      provides: "Rate limiting logic"
    - path: "frontend-next/app/api/generate/route.ts"
      provides: "Proxy API Route"
  key_links:
    - from: "Next.js Proxy API"
      to: "Python API"
      via: "fetch() with forwarded Authorization header"
---

<objective>
Implement Next.js proxy endpoints that validate inputs with Zod and apply Upstash Redis rate limiting via middleware before forwarding valid requests to the hardened Python backend.

Purpose: Shield the backend from malformed payloads and abuse, centralizing security in the Next.js DMZ.
Output: Secure Next.js API routes handling rate limiting and Zod validation.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Zod Validation Schemas</name>
  <files>frontend-next/package.json, frontend-next/lib/validations/api.ts</files>
  <action>
    Install `zod`.
    Create `frontend-next/lib/validations/api.ts`.
    Define Zod schemas matching the expected inputs for generation endpoints (e.g., `GeneratePostSchema` requiring fields like `topic`, `hook_style`, `cta_style`, `author_id`, etc., based on the python Pydantic models).
  </action>
  <verify>
    <automated>cd frontend-next && npx tsc --noEmit</automated>
  </verify>
  <done>Zod schemas accurately map the required parameters for the backend generation process.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Upstash Rate Limiting in Middleware</name>
  <files>frontend-next/package.json, frontend-next/middleware.ts</files>
  <action>
    Install `@upstash/ratelimit` and `@upstash/redis`.
    Update `frontend-next/middleware.ts` to include an Upstash rate limiter logic (e.g., 10 requests per minute per user ID/IP) for routes starting with `/api/`.
    Ensure the Supabase session update logic created in Plan 1 is maintained alongside the new rate limit logic.
  </action>
  <verify>
    <automated>cd frontend-next && npx tsc --noEmit</automated>
  </verify>
  <done>Middleware correctly processes rate limiting and session refresh without type errors.</done>
</task>

<task type="auto">
  <name>Task 3: Build Validating API Proxy Route</name>
  <files>frontend-next/app/api/generate/route.ts</files>
  <action>
    Create a POST handler at `frontend-next/app/api/generate/route.ts`.
    1. Parse the JSON body.
    2. Validate it using the Zod schemas from Task 1. Return 400 with Zod errors if invalid.
    3. Retrieve the user's Supabase session/token. Return 401 if unauthenticated.
    4. Forward the valid request payload to the Python backend's generation endpoint via `fetch()`, injecting the `Authorization: Bearer <token>` header.
    5. Return the proxy response back to the client.
  </action>
  <verify>
    <automated>cd frontend-next && npx tsc --noEmit</automated>
  </verify>
  <done>Proxy route validates using Zod, enforces Auth, and successfully proxies to the local Python backend.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint</name>
  <action>Pause for human check</action>
  <files>N/A</files>
  <verify>Human visual check</verify>
  <done>Human approved</done>
  <what-built>Zod validation and Upstash rate limiting via Next.js proxy.</what-built>
  <how-to-verify>
    1. Add `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` to `frontend-next/.env.local`.
    2. Start the frontend `npm run dev` and backend `python server.py`.
    3. Send an invalid POST request to `http://localhost:3000/api/generate` (e.g., missing topic). Verify you get a 400 Zod error.
    4. Send >10 valid requests to trigger the rate limit. Verify you receive a 429 Too Many Requests response.
  </how-to-verify>
  <resume-signal>Type "approved" once you have verified Zod rejection and Rate limiting.</resume-signal>
</task>

</tasks>

<verification>
Check that malformed inputs to `/api/generate` never reach the Python server and are blocked by Zod in Next.js, and spamming the API triggers rate limit blocks.
</verification>

<success_criteria>
Zod strictly validates all inputs, Upstash prevents abuse, and requests securely proxy to Python.
</success_criteria>

<output>
After completion, create `.planning/phases/01-the-security-dmz/01-03-SUMMARY.md`
</output>
