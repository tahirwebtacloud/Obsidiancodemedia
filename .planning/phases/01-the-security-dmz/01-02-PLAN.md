---
phase: 01-the-security-dmz
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - server.py
  - .env.example
autonomous: true
requirements: [SEC-03]

must_haves:
  truths:
    - "Python API strictly requires a valid authentication token"
    - "AUTH_BYPASS logic is entirely removed from the codebase"
  artifacts:
    - path: "server.py"
      provides: "FastAPI server"
  key_links:
    - from: "Incoming Request"
      to: "server.py get_user_id"
      via: "Authorization: Bearer <token>"
---

<objective>
Remove all development auth bypasses and enforce strict JWT validation in the FastAPI server.

Purpose: Prevent unauthenticated access to the underlying python APIs and database logic by relying strictly on valid Supabase JWTs.
Output: Hardened Python backend that completely rejects unauthenticated requests.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove AUTH_BYPASS from Configuration</name>
  <files>server.py, .env.example</files>
  <action>
    Remove `_AUTH_BYPASS` constant definition and reading from environment variables in `server.py`.
    Remove any print warnings related to `AUTH_BYPASS`.
    Remove `AUTH_BYPASS=true` or similar lines from `.env.example`.
  </action>
  <verify>
    <automated>! grep -q "AUTH_BYPASS" server.py</automated>
  </verify>
  <done>AUTH_BYPASS config completely scrubbed from server config.</done>
</task>

<task type="auto">
  <name>Task 2: Hard-code Strict Auth in get_user_id()</name>
  <files>server.py</files>
  <action>
    Modify `get_user_id(request)` or equivalent auth function to strictly check for the `Authorization: Bearer` token.
    Remove all branches that fallback to reading `X-User-ID` or return dummy users like "00000000-0000-0000-0000-000000000000".
    If the JWT is missing or invalid, immediately raise `HTTPException(status_code=401, detail="Unauthorized")`.
  </action>
  <verify>
    <automated>pytest -c /dev/null --version || echo "No tests configured yet, run manual python syntax check: python -m py_compile server.py"</automated>
  </verify>
  <done>Only requests with valid JWTs can successfully resolve a user ID.</done>
</task>

</tasks>

<verification>
Start `server.py` and attempt an unauthenticated request to an endpoint. It must return a 401 Unauthorized error.
</verification>

<success_criteria>
`AUTH_BYPASS` string no longer exists in the codebase and the python server is effectively locked down.
</success_criteria>

<output>
After completion, create `.planning/phases/01-the-security-dmz/01-02-SUMMARY.md`
</output>
